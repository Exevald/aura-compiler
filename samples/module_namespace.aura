module samples.module_namespace;

import samples.math_utils as math;
import std.io as io;

var left: int = 20;
var right: int = 7;

io.println("Imported module samples.math_utils");
io.println("sum:", math.sum(left, right));
io.println("difference:", math.difference(left, right));