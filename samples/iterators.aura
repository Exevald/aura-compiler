var nums = [1, 2, 3, 4, 5, 6, 7, 8];

print "Take 5 elements, then reverse them:";
iter (n of nums with [take(5), reverse]) {
    print n;
}

print "Drop 3 elements:";
iter (n of nums with [drop(3)]) {
    print n;
}