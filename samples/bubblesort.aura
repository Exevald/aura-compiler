module samples.bubblesort;

import samples.sorting;
import std.io as io;
import std.array;

var list = [5, 2, 9, 1, 5, 6];
io.println("Original list:", list);

var sorted = sorting.bubbleSort(list, 6);
io.println("Sorted array:", sorted);