module samples.factorial;

import std.io as io;

fn factorial(n: int) : int {
    if (n == 0) {
        return 1;
    }
    return n * factorial(n - 1);
}

io.println("factorial(5) =", factorial(5));
